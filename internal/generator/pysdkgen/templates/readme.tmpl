# {{.PackageName}}

Generated sync Python SDK for the **{{.SchemaName}}** API schema.

> Auto-generated by `superschematic` (`pysdkgen`). Do not edit manually.

## Install

```bash
pip install {{.PackageName}}
```

## Quick Start

```python
from {{.PackageName}} import ClientConfig, {{.SDKClassName}}

config = ClientConfig(base_url="https://api.example.com")
sdk = {{.SDKClassName}}(config)
```

## Notes

- Runtime is sync-first and uses the standard library HTTP client.
- Runtime dependency: `pydantic` (strict scalar validation).
- Optional: `pip install {{.PackageName}}[encryption]` to enable encrypted payload algorithms (`cryptography>=42.0.0`).
- Install the generated types package `{{.TypesPackage}}` to enable model-based pre-request validation.
- Auth can be configured per-endpoint, with optional token refresh + single retry on `401`.
- Rate-limit failures expose `retry_after` when the API includes a `Retry-After` header.
- Encrypted endpoints support option plumbing via `EncryptedRequestOptions`.
- File uploads use `FilePart` and multipart form requests.

## Generated Information

- **Schema:** {{.SchemaName}}
- **Generated:** {{.Timestamp}}
