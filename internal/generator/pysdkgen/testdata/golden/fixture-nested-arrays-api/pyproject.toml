# Code generated by pysdkgen. DO NOT EDIT.

[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "schemas_fixture_nested_arrays_api_sdk"
version = "1.0.0"
description = "Generated Python SDK for fixture-nested-arrays-api API schema"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
    {name = "superschematic"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.12.0,<3.0.0",
]

[project.optional-dependencies]
encryption = [
    "cryptography>=42.0.0",
]

[tool.setuptools]
packages = ["schemas_fixture_nested_arrays_api_sdk", "schemas_fixture_nested_arrays_api_sdk.namespaces"]

[tool.setuptools.package-data]
"schemas_fixture_nested_arrays_api_sdk" = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
