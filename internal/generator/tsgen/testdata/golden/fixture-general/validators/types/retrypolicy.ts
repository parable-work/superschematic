// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { load as loadYaml } from 'js-yaml';
import type { RetryPolicy } from '../../types';

/**
 * Validates a RetryPolicy object
 */
export function validateRetryPolicy(value: RetryPolicy | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (value.maxAttempts === null || value.maxAttempts === undefined) {
    addFieldError(errors, "maxAttempts", "required", "required field");
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateRetryPolicyRequired(value: RetryPolicy | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateRetryPolicy(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const RetryPolicyKnownFields = new Set<string>([
  'maxAttempts',
  'backoffSeconds',
]);

type RetryPolicyTextParser = (value: string) => unknown;

function parseRetryPolicyInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: RetryPolicyTextParser
): RetryPolicy {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseRetryPolicy ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseRetryPolicy ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !RetryPolicyKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseRetryPolicy ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as RetryPolicy;
  const validationResult = validateRetryPolicy(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseRetryPolicy ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseRetryPolicyJson(input: string | unknown): RetryPolicy {
  return parseRetryPolicyInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseRetryPolicyJsonNonStrict(input: string | unknown): RetryPolicy {
  return parseRetryPolicyInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseRetryPolicyYaml(input: string | unknown): RetryPolicy {
  return parseRetryPolicyInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseRetryPolicyYamlNonStrict(input: string | unknown): RetryPolicy {
  return parseRetryPolicyInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a RetryPolicy from JSON wire format into runtime types
 */

export function parseRetryPolicyFromJSON(input: unknown): RetryPolicy {
  return input as RetryPolicy;
}
