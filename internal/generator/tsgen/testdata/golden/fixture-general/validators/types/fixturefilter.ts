// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from '@psgen/scalar-lib/validation';
import type { ScalarValidationResult, ValidationResult } from '@psgen/scalar-lib/validation';
import { load as loadYaml } from 'js-yaml';
import type { FixtureFilter } from '../../types';

/**
 * Validates a FixtureFilter object
 */
export function validateFixtureFilter(value: FixtureFilter | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (value.kind === null || value.kind === undefined) {
    addFieldError(errors, "kind", "required", "required field");
  }

  {
    const fieldValue = value.kind;

    if (String(fieldValue).length > 32) {
      addFieldError(errors, "kind", "maxLength", "must be at most 32 characters");
    }

  }

  if (Array.isArray(value.values)) {
    const fieldValue = value.values;

    fieldValue.forEach((item, index) => {
      if (String(item).length > 64) {
        addFieldError(errors, `values[${index}]`, "maxLength", "must be at most 64 characters");
      }
    });

    if (fieldValue.length > 10) {
      addFieldError(errors, "values", "listMax", "must contain at most 10 items");
    }

    if (fieldValue.length < 1) {
      addFieldError(errors, "values", "listMin", "must contain at least 1 items");
    }

  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateFixtureFilterRequired(value: FixtureFilter | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateFixtureFilter(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const FixtureFilterKnownFields = new Set<string>([
  'kind',
  'values',
]);

type FixtureFilterTextParser = (value: string) => unknown;

function parseFixtureFilterInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: FixtureFilterTextParser
): FixtureFilter {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseFixtureFilter ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseFixtureFilter ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !FixtureFilterKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseFixtureFilter ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as FixtureFilter;
  const validationResult = validateFixtureFilter(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseFixtureFilter ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseFixtureFilterJson(input: string | unknown): FixtureFilter {
  return parseFixtureFilterInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseFixtureFilterJsonNonStrict(input: string | unknown): FixtureFilter {
  return parseFixtureFilterInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseFixtureFilterYaml(input: string | unknown): FixtureFilter {
  return parseFixtureFilterInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseFixtureFilterYamlNonStrict(input: string | unknown): FixtureFilter {
  return parseFixtureFilterInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a FixtureFilter from JSON wire format into runtime types
 */

export function parseFixtureFilterFromJSON(input: unknown): FixtureFilter {
  return input as FixtureFilter;
}
