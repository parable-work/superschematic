// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { expectNumber, expectString } from '../primitives';
import { load as loadYaml } from 'js-yaml';
import type { FixtureConfig, FixtureEnvironment } from '../../types';

import { validateNetworkUrlRequired, validateNetworkUrl } from '../scalars/network_url';

import { validateFixtureEnvironmentRequired, validateFixtureEnvironment } from '../enums';

/**
 * Validates a FixtureConfig object
 */
export function validateFixtureConfig(value: FixtureConfig | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateNetworkUrlRequired(value.DATABASE_URL);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "DATABASE_URL", fieldErrors);
    }
  }

  if (value.JWT_SECRET === null || value.JWT_SECRET === undefined) {
    addFieldError(errors, "JWT_SECRET", "required", "required field");
  }

  expectString(errors, "JWT_SECRET", value.JWT_SECRET);

  if (value.PORT === null || value.PORT === undefined) {
    addFieldError(errors, "PORT", "required", "required field");
  }

  expectNumber(errors, "PORT", value.PORT);

  {
    const [valid, fieldErrors] = validateFixtureEnvironmentRequired(value.ENVIRONMENT);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "ENVIRONMENT", fieldErrors);
    }
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateFixtureConfigRequired(value: FixtureConfig | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateFixtureConfig(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const FixtureConfigKnownFields = new Set<string>([
  'DATABASE_URL',
  'JWT_SECRET',
  'PORT',
  'ENVIRONMENT',
]);

type FixtureConfigTextParser = (value: string) => unknown;

function parseFixtureConfigInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: FixtureConfigTextParser
): FixtureConfig {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseFixtureConfig ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseFixtureConfig ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (candidate["PORT"] === undefined || candidate["PORT"] === null) {
    candidate["PORT"] = 8080;
  }

  if (candidate["ENVIRONMENT"] === undefined || candidate["ENVIRONMENT"] === null) {
    candidate["ENVIRONMENT"] = "development" as FixtureEnvironment;
  }

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !FixtureConfigKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseFixtureConfig ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as FixtureConfig;
  const validationResult = validateFixtureConfig(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseFixtureConfig ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseFixtureConfigJson(input: string | unknown): FixtureConfig {
  return parseFixtureConfigInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseFixtureConfigJsonNonStrict(input: string | unknown): FixtureConfig {
  return parseFixtureConfigInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseFixtureConfigYaml(input: string | unknown): FixtureConfig {
  return parseFixtureConfigInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseFixtureConfigYamlNonStrict(input: string | unknown): FixtureConfig {
  return parseFixtureConfigInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a FixtureConfig from JSON wire format into runtime types
 */

export function parseFixtureConfigFromJSON(input: unknown): FixtureConfig {
  return input as FixtureConfig;
}
