// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from '@psgen/scalar-lib/validation';
import {
  FixtureEnvironment,
} from '../types';

/**
 * Checks if a value is a valid FixtureEnvironment
 */
export function isValidFixtureEnvironment(value: unknown): value is FixtureEnvironment {
  return Object.values(FixtureEnvironment).includes(value as FixtureEnvironment);
}

/**
 * Validates a FixtureEnvironment value (optional field)
 */
export function validateFixtureEnvironment(
  value: FixtureEnvironment | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  if (!isValidFixtureEnvironment(value)) {
    errors.push({ validator: "enum", message: `must be a valid FixtureEnvironment value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a FixtureEnvironment value (required field)
 */
export function validateFixtureEnvironmentRequired(
  value: FixtureEnvironment | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    errors.push({ validator: "required", message: "required field" });
    return [false, errors];
  }

  if (!isValidFixtureEnvironment(value)) {
    errors.push({ validator: "enum", message: `must be a valid FixtureEnvironment value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}
