// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from '@psgen/scalar-lib/validation';

import type { JSDate } from '../../types';

import { parseTemporalDateTime as parseTemporalDateTimeFromLib } from '@psgen/scalar-lib/scalars';

/**
 * Validates a Temporal.DateTime value
 */
export function validateTemporalDateTime(
  value: JSDate | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  const s = String(value);

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a Temporal.DateTime value with required check
 */
export function validateTemporalDateTimeRequired(
  value: JSDate | null | undefined
): ScalarValidationResult {

  if (value === null || value === undefined) {
    return [false, [{ validator: "required", message: "required field" }]];
  }

  return validateTemporalDateTime(value);
}

/**
 * Parses a string and returns a Temporal.DateTime scalar
 */
export function parseTemporalDateTime(value: string): JSDate | null {
  return parseTemporalDateTimeFromLib(value);
}
