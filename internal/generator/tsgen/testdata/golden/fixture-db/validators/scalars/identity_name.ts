// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from 'superscalar/validation';

/**
 * Validates a Identity.Name value
 */
export function validateIdentityName(
  value: string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  // A value of another JSON type is "type", and its length and format are
  // not checked.
  if (typeof value !== "string") {
    return [false, [{ validator: "type", message: "expected string value" }]];
  }
  const s = value;

  if (s.length < 2) {
    errors.push({ validator: "minLength", message: "must be at least 2 characters" });
  }

  if (s.length > 80) {
    errors.push({ validator: "maxLength", message: "must be at most 80 characters" });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a Identity.Name value with required check
 */
export function validateIdentityNameRequired(
  value: string | null | undefined
): ScalarValidationResult {

  if (value === null || value === undefined || value === "") {
    return [false, [{ validator: "required", message: "required field" }]];
  }

  return validateIdentityName(value);
}
