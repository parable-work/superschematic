// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from '@psgen/scalar-lib/validation';
import {
  TenantStatus,
} from '../types';

/**
 * Checks if a value is a valid TenantStatus
 */
export function isValidTenantStatus(value: unknown): value is TenantStatus {
  return Object.values(TenantStatus).includes(value as TenantStatus);
}

/**
 * Validates a TenantStatus value (optional field)
 */
export function validateTenantStatus(
  value: TenantStatus | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  if (!isValidTenantStatus(value)) {
    errors.push({ validator: "enum", message: `must be a valid TenantStatus value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a TenantStatus value (required field)
 */
export function validateTenantStatusRequired(
  value: TenantStatus | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    errors.push({ validator: "required", message: "required field" });
    return [false, errors];
  }

  if (!isValidTenantStatus(value)) {
    errors.push({ validator: "enum", message: `must be a valid TenantStatus value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}
