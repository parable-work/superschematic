// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
  addNestedErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { expectNumber, expectString } from '../primitives';
import { load as loadYaml } from 'js-yaml';
import type { TenantUser, JSDate, Tenant } from '../../types';

import { validateTemporalDateTimeRequired, validateTemporalDateTime } from '../scalars/temporal_date_time';

import { validateIdentityUUIDRequired, validateIdentityUUID } from '../scalars/identity_uuid';

import { parseTemporalDateTime as parseTemporalDateTimeFromLib } from 'superscalar/scalars';

import { parseTenantFromJSON, validateTenant } from './tenant';

/**
 * Validates a TenantUser object
 */
export function validateTenantUser(value: TenantUser | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateTemporalDateTimeRequired(value.createdAt);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "createdAt", fieldErrors);
    }
  }

  if (value.updatedAt !== null && value.updatedAt !== undefined) {
    const [valid, fieldErrors] = validateTemporalDateTime(value.updatedAt);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "updatedAt", fieldErrors);
    }
  }

  if (value.id !== null && value.id !== undefined) {
    const [valid, fieldErrors] = validateIdentityUUID(value.id);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "id", fieldErrors);
    }
  }

  {
    // A nested object is validated as its own type, its errors under the
    // field's path; any other value is left to the field's other checks.
    const validateNested = (nested: unknown, path: string) => {
      if (nested === null || typeof nested !== 'object' || Array.isArray(nested)) {
        return;
      }
      const nestedErrors = validateTenant(nested as Tenant);
      if (nestedErrors !== true) {
        addNestedErrors(errors, path, nestedErrors);
      }
    };

    validateNested(value.tenant, "tenant");

  }

  if (value.tenant === null || value.tenant === undefined) {
    addFieldError(errors, "tenant", "required", "required field");
  }

  expectString(errors, "displayName", value.displayName);

  if (value.deletedAt !== null && value.deletedAt !== undefined) {
    const [valid, fieldErrors] = validateTemporalDateTime(value.deletedAt);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "deletedAt", fieldErrors);
    }
  }

  if (value.deletedBy !== null && value.deletedBy !== undefined) {
    const [valid, fieldErrors] = validateIdentityUUID(value.deletedBy);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "deletedBy", fieldErrors);
    }
  }

  expectNumber(errors, "_version", value._version);

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateTenantUserRequired(value: TenantUser | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateTenantUser(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const TenantUserKnownFields = new Set<string>([
  'createdAt',
  'updatedAt',
  'id',
  'tenant',
  'displayName',
  'deletedAt',
  'deletedBy',
  '_version',
]);

type TenantUserTextParser = (value: string) => unknown;

function parseTenantUserInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: TenantUserTextParser
): TenantUser {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseTenantUser ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseTenantUser ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !TenantUserKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseTenantUser ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as TenantUser;
  const validationResult = validateTenantUser(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseTenantUser ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseTenantUserJson(input: string | unknown): TenantUser {
  return parseTenantUserInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantUserJsonNonStrict(input: string | unknown): TenantUser {
  return parseTenantUserInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantUserYaml(input: string | unknown): TenantUser {
  return parseTenantUserInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseTenantUserYamlNonStrict(input: string | unknown): TenantUser {
  return parseTenantUserInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a TenantUser from JSON wire format into runtime types
 */

export function parseTenantUserFromJSON(input: unknown): TenantUser {
  if (input == null || typeof input !== 'object') {
    return input as TenantUser;
  }
  const obj = input as Record<string, unknown>;
  return {
    ...(obj as unknown as TenantUser),

    createdAt: obj.createdAt != null
      ? (parseTemporalDateTimeFromLib(obj.createdAt as string) as JSDate)
      : (obj.createdAt as unknown as JSDate),

    updatedAt: obj.updatedAt != null
      ? (parseTemporalDateTimeFromLib(obj.updatedAt as string) as JSDate)
      : (obj.updatedAt as unknown as JSDate | null),

    tenant: obj.tenant != null
      ? parseTenantFromJSON(obj.tenant)
      : (obj.tenant as unknown as Tenant),

    deletedAt: obj.deletedAt != null
      ? (parseTemporalDateTimeFromLib(obj.deletedAt as string) as JSDate)
      : (obj.deletedAt as unknown as JSDate | null),

  };
}
