// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
  addNestedErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { load as loadYaml } from 'js-yaml';
import type { Tenant, JSDate, TenantUser, TenantStatus } from '../../types';

import { validateTemporalDateTimeRequired, validateTemporalDateTime } from '../scalars/temporal_date_time';

import { validateIdentityUUIDRequired, validateIdentityUUID } from '../scalars/identity_uuid';

import { validateIdentityNameRequired, validateIdentityName } from '../scalars/identity_name';

import { validateIdentitySlugRequired, validateIdentitySlug } from '../scalars/identity_slug';

import { validateGenericJSONRequired, validateGenericJSON } from '../scalars/generic_json';

import { validateTenantStatusRequired, validateTenantStatus } from '../enums';

import { parseTemporalDateTime as parseTemporalDateTimeFromLib } from 'superscalar/scalars';

import { parseTenantUserFromJSON, validateTenantUser } from './tenantuser';

/**
 * Validates a Tenant object
 */
export function validateTenant(value: Tenant | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateTemporalDateTimeRequired(value.createdAt);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "createdAt", fieldErrors);
    }
  }

  if (value.updatedAt !== null && value.updatedAt !== undefined) {
    const [valid, fieldErrors] = validateTemporalDateTime(value.updatedAt);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "updatedAt", fieldErrors);
    }
  }

  if (value.id !== null && value.id !== undefined) {
    const [valid, fieldErrors] = validateIdentityUUID(value.id);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "id", fieldErrors);
    }
  }

  {
    const [valid, fieldErrors] = validateIdentityNameRequired(value.name);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "name", fieldErrors);
    }
  }

  {
    const [valid, fieldErrors] = validateIdentitySlugRequired(value.slug);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "slug", fieldErrors);
    }
  }

  if (value.email === null || value.email === undefined) {
    addFieldError(errors, "email", "required", "required field");
  }

  {
    const fieldValue = value.email;

    if (!(new RegExp("@")).test(String(fieldValue))) {
      addFieldError(errors, "email", "pattern", "invalid format");
    }

  }

  {
    const [valid, fieldErrors] = validateTenantStatusRequired(value.status);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "status", fieldErrors);
    }
  }

  if (value.isActive === null || value.isActive === undefined) {
    addFieldError(errors, "isActive", "required", "required field");
  }

  if (value.seatCount === null || value.seatCount === undefined) {
    addFieldError(errors, "seatCount", "required", "required field");
  }

  {
    const [valid, fieldErrors] = validateGenericJSONRequired(value.metadata);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "metadata", fieldErrors);
    }
  }

  {
    // A nested object is validated as its own type, its errors under the
    // field's path; any other value is left to the field's other checks.
    const validateNested = (nested: unknown, path: string) => {
      if (nested === null || typeof nested !== 'object' || Array.isArray(nested)) {
        return;
      }
      const nestedErrors = validateTenantUser(nested as TenantUser);
      if (nestedErrors !== true) {
        addNestedErrors(errors, path, nestedErrors);
      }
    };

    if (Array.isArray(value.users)) {
      value.users.forEach((item, index) => validateNested(item, `users[${index}]`));
    }

  }

  if (value.users === null || value.users === undefined) {
    addFieldError(errors, "users", "required", "required field");
  }

  // A list element is never null: its one error is "required", in place of
  // whatever the element checks above made of it.
  if (Array.isArray(value.users)) {
    value.users.forEach((item, index) => {
      if (item === null || item === undefined) {
        setFieldErrors(errors, `users[${index}]`, [{ validator: "required", message: "required field" }]);
      }
    });
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateTenantRequired(value: Tenant | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateTenant(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const TenantKnownFields = new Set<string>([
  'createdAt',
  'updatedAt',
  'id',
  'name',
  'slug',
  'email',
  'status',
  'isActive',
  'seatCount',
  'metadata',
  'users',
  '_version',
]);

type TenantTextParser = (value: string) => unknown;

function parseTenantInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: TenantTextParser
): Tenant {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseTenant ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseTenant ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (candidate["status"] === undefined || candidate["status"] === null) {
    candidate["status"] = "active" as TenantStatus;
  }

  if (candidate["isActive"] === undefined || candidate["isActive"] === null) {
    candidate["isActive"] = true;
  }

  if (candidate["seatCount"] === undefined || candidate["seatCount"] === null) {
    candidate["seatCount"] = 5;
  }

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !TenantKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseTenant ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as Tenant;
  const validationResult = validateTenant(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseTenant ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseTenantJson(input: string | unknown): Tenant {
  return parseTenantInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantJsonNonStrict(input: string | unknown): Tenant {
  return parseTenantInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantYaml(input: string | unknown): Tenant {
  return parseTenantInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseTenantYamlNonStrict(input: string | unknown): Tenant {
  return parseTenantInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a Tenant from JSON wire format into runtime types
 */

export function parseTenantFromJSON(input: unknown): Tenant {
  if (input == null || typeof input !== 'object') {
    return input as Tenant;
  }
  const obj = input as Record<string, unknown>;
  return {
    ...(obj as unknown as Tenant),

    createdAt: obj.createdAt != null
      ? (parseTemporalDateTimeFromLib(obj.createdAt as string) as JSDate)
      : (obj.createdAt as unknown as JSDate),

    updatedAt: obj.updatedAt != null
      ? (parseTemporalDateTimeFromLib(obj.updatedAt as string) as JSDate)
      : (obj.updatedAt as unknown as JSDate | null),

    users: Array.isArray(obj.users)
      ? (obj.users as unknown[]).map((item) => parseTenantUserFromJSON(item))
      : (obj.users as TenantUser[]),

  };
}
