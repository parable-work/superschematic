// Code generated by tsgen. DO NOT EDIT.

import { addFieldError } from 'superscalar/validation';
import type { ValidationErrors } from 'superscalar/validation';

// A field typed with a builtin primitive must hold a value of that JSON
// type. Each expect* check reports "type" at path for a present value of
// another type and leaves null and undefined to the presence checks; the
// field's length, pattern and range rules skip such a value, so it is one
// error.

export function expectString(errors: ValidationErrors, path: string, value: unknown): void {
  if (value !== null && value !== undefined && typeof value !== "string") {
    addFieldError(errors, path, "type", "expected a string");
  }
}

export function expectNumber(errors: ValidationErrors, path: string, value: unknown): void {
  if (value !== null && value !== undefined && !isFiniteNumber(value)) {
    addFieldError(errors, path, "type", "expected a number");
  }
}

export function expectBoolean(errors: ValidationErrors, path: string, value: unknown): void {
  if (value !== null && value !== undefined && typeof value !== "boolean") {
    addFieldError(errors, path, "type", "expected a boolean");
  }
}

// isFiniteNumber is true for a number that is neither NaN nor infinite: the
// values a range rule (min, max) checks.
export function isFiniteNumber(value: unknown): value is number {
  return typeof value === "number" && Number.isFinite(value);
}
