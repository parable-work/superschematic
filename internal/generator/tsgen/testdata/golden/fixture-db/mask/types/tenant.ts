// Code generated by tsgen. DO NOT EDIT.

import type {
  Tenant,
  TenantUser,
} from '../../types';
import { maskSecretsTenantUser } from './tenantuser';

/**
 * Returns a copy of Tenant with fields marked @secret cleared.
 */
export function maskSecretsTenant(value: Tenant): Tenant {
  return {
    createdAt: value.createdAt,
    updatedAt: value.updatedAt,
    id: value.id,
    name: value.name,
    slug: value.slug,
    email: value.email,
    status: value.status,
    isActive: value.isActive,
    seatCount: value.seatCount,
    metadata: value.metadata,
    users: Array.isArray(value.users) ? value.users.map((item: TenantUser) => maskSecretsTenantUser(item)) : [],
    _version: value._version,
  };
}
