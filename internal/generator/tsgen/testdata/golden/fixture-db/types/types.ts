// Code generated by tsgen. DO NOT EDIT.

import type {
  JSDate,
  GenericJSON,
  IdentityName,
  IdentitySlug,
  IdentityUUID,
  TemporalDateTime,
} from './scalars';
import type {
  TenantStatus,
} from './enums';

export interface HistoryRecord<T> {
  version: number;
  operation: string;
  recordedAt: JSDate;
  value: T;
}

/**
 * Auditable - Base class providing audit fields to every table.
 */
export interface Auditable {
  createdAt: JSDate;
  updatedAt?: JSDate | null;
}

/**
 * Tenant - A tenant of the platform.
 */
export interface Tenant {
  createdAt: JSDate;
  updatedAt?: JSDate | null;
  id?: string | null;
  /** Display name shown across the product. */
  name: string;
  slug: string;
  email: string;
  status: TenantStatus;
  isActive: boolean;
  seatCount: number;
  metadata: Record<string, any>;
  users: TenantUser[];
  _version: number;
}

/**
 * TenantDefaults holds @default values declared in the schema. Defaults
 * are typed as Partial<Tenant> so they can be merged into incoming
 * payloads or used to bootstrap a new instance.
 */
export const TenantDefaults: Partial<Tenant> = {
  status: "active" as TenantStatus,
  isActive: true,
  seatCount: 5,
};

/**
 * makeTenant returns a Tenant initialised with @default values from
 * the schema. Caller-provided overrides win over the declared defaults.
 */
export function makeTenant(overrides?: Partial<Tenant>): Tenant {
  return { ...TenantDefaults, ...(overrides || {}) } as Tenant;
}

/**
 * TenantUser - A soft-deletable tenant membership.
 */
export interface TenantUser {
  createdAt: JSDate;
  updatedAt?: JSDate | null;
  id?: string | null;
  tenant: Tenant;
  displayName?: string | null;
  deletedAt?: JSDate | null;
  deletedBy?: string | null;
  _version: number;
}
