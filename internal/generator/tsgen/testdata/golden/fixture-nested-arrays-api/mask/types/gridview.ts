// Code generated by tsgen. DO NOT EDIT.

import type {
  GridView,
  Point,
} from '../../types';
import { maskSecretsPoint } from './point';

/**
 * Returns a copy of GridView with fields marked @secret cleared.
 */
export function maskSecretsGridView(value: GridView): GridView {
  return {
    id: value.id,
    labels: Array.isArray(value.labels) ? value.labels : [],
    shades: Array.isArray(value.shades) ? value.shades : [],
    polygons: Array.isArray(value.polygons) ? value.polygons.map((row: Point[]) => Array.isArray(row) ? row.map((item: Point) => maskSecretsPoint(item)) : row) : [],
    weights: Array.isArray(value.weights) ? value.weights : null,
  };
}
