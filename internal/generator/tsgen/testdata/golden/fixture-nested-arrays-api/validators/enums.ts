// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from 'superscalar/validation';
import {
  Shade,
} from '../types';

/**
 * Checks if a value is a valid Shade
 */
export function isValidShade(value: unknown): value is Shade {
  return Object.values(Shade).includes(value as Shade);
}

/**
 * Validates a Shade value (optional field)
 */
export function validateShade(
  value: Shade | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  if (!isValidShade(value)) {
    errors.push({ validator: "enum", message: `must be a valid Shade value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a Shade value (required field)
 */
export function validateShadeRequired(
  value: Shade | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    errors.push({ validator: "required", message: "required field" });
    return [false, errors];
  }

  if (!isValidShade(value)) {
    errors.push({ validator: "enum", message: `must be a valid Shade value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}
