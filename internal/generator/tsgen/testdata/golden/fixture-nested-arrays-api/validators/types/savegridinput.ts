// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { load as loadYaml } from 'js-yaml';
import type { SaveGridInput, Point } from '../../types';

import { validateShadeRequired, validateShade } from '../enums';

import { parsePointFromJSON } from './point';

/**
 * Validates a SaveGridInput object
 */
export function validateSaveGridInput(value: SaveGridInput | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (Array.isArray(value.labels)) {
    value.labels.forEach((row, rowIndex) => {
      if (!Array.isArray(row)) {
        addFieldError(errors, `labels[${rowIndex}]`, "required", "inner list must be an array");
      }
    });
  }

  if (value.labels === null || value.labels === undefined) {
    addFieldError(errors, "labels", "required", "required field");
  }

  if (Array.isArray(value.shades)) {
    value.shades.forEach((row, rowIndex) => {
      if (!Array.isArray(row)) {
        addFieldError(errors, `shades[${rowIndex}]`, "required", "inner list must be an array");
        return;
      }
      row.forEach((item, index) => {
        const [valid, fieldErrors] = validateShadeRequired(item);
        if (!valid && fieldErrors) {
          setFieldErrors(errors, `shades[${rowIndex}][${index}]`, fieldErrors);
        }
      });
    });
  } else {
    addFieldError(errors, "shades", "required", "required field");
  }

  if (Array.isArray(value.polygons)) {
    value.polygons.forEach((row, rowIndex) => {
      if (!Array.isArray(row)) {
        addFieldError(errors, `polygons[${rowIndex}]`, "required", "inner list must be an array");
      }
    });
  }

  if (value.polygons === null || value.polygons === undefined) {
    addFieldError(errors, "polygons", "required", "required field");
  }

  if (Array.isArray(value.weights)) {
    value.weights.forEach((row, rowIndex) => {
      if (!Array.isArray(row)) {
        addFieldError(errors, `weights[${rowIndex}]`, "required", "inner list must be an array");
      }
    });
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateSaveGridInputRequired(value: SaveGridInput | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateSaveGridInput(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const SaveGridInputKnownFields = new Set<string>([
  'labels',
  'shades',
  'polygons',
  'weights',
]);

type SaveGridInputTextParser = (value: string) => unknown;

function parseSaveGridInputInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: SaveGridInputTextParser
): SaveGridInput {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseSaveGridInput ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseSaveGridInput ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !SaveGridInputKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseSaveGridInput ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as SaveGridInput;
  const validationResult = validateSaveGridInput(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseSaveGridInput ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseSaveGridInputJson(input: string | unknown): SaveGridInput {
  return parseSaveGridInputInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseSaveGridInputJsonNonStrict(input: string | unknown): SaveGridInput {
  return parseSaveGridInputInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseSaveGridInputYaml(input: string | unknown): SaveGridInput {
  return parseSaveGridInputInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseSaveGridInputYamlNonStrict(input: string | unknown): SaveGridInput {
  return parseSaveGridInputInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a SaveGridInput from JSON wire format into runtime types
 */

export function parseSaveGridInputFromJSON(input: unknown): SaveGridInput {
  if (input == null || typeof input !== 'object') {
    return input as SaveGridInput;
  }
  const obj = input as Record<string, unknown>;
  return {
    ...(obj as unknown as SaveGridInput),

    polygons: Array.isArray(obj.polygons)
      ? (obj.polygons as unknown[]).map((row) =>
          Array.isArray(row) ? row.map((item) => parsePointFromJSON(item)) : (row as Point[])
        )
      : (obj.polygons as Point[][]),

  };
}
