// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
  addNestedErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { load as loadYaml } from 'js-yaml';
import type { GridView, Point } from '../../types';

import { validateIdentityUUIDRequired, validateIdentityUUID } from '../scalars/identity_uuid';

import { validateShadeRequired, validateShade } from '../enums';

import { parsePointFromJSON, validatePoint } from './point';

/**
 * Validates a GridView object
 */
export function validateGridView(value: GridView | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateIdentityUUIDRequired(value.id);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "id", fieldErrors);
    }
  }

  if (Array.isArray(value.labels)) {
    value.labels.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `labels[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `labels[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  if (value.labels === null || value.labels === undefined) {
    addFieldError(errors, "labels", "required", "required field");
  }

  // A list element is never null: its one error is "required", in place of
  // whatever the element checks above made of it.
  if (Array.isArray(value.labels)) {
    value.labels.forEach((row, rowIndex) => {
      if (Array.isArray(row)) {
        row.forEach((item, index) => {
          if (item === null || item === undefined) {
            setFieldErrors(errors, `labels[${rowIndex}][${index}]`, [{ validator: "required", message: "required field" }]);
          }
        });
      }
    });
  }

  if (Array.isArray(value.shades)) {
    value.shades.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `shades[${rowIndex}]`, "required", "required field");
        return;
      }
      if (!Array.isArray(row)) {
        addFieldError(errors, `shades[${rowIndex}]`, "type", "expected an array");
        return;
      }
      row.forEach((item, index) => {
        const [valid, fieldErrors] = validateShadeRequired(item);
        if (!valid && fieldErrors) {
          setFieldErrors(errors, `shades[${rowIndex}][${index}]`, fieldErrors);
        }
      });
    });
  } else {
    addFieldError(errors, "shades", "required", "required field");
  }

  // A list element is never null: its one error is "required", in place of
  // whatever the element checks above made of it.
  if (Array.isArray(value.shades)) {
    value.shades.forEach((row, rowIndex) => {
      if (Array.isArray(row)) {
        row.forEach((item, index) => {
          if (item === null || item === undefined) {
            setFieldErrors(errors, `shades[${rowIndex}][${index}]`, [{ validator: "required", message: "required field" }]);
          }
        });
      }
    });
  }

  {
    // A nested object is validated as its own type, its errors under the
    // field's path; any other value is left to the field's other checks.
    const validateNested = (nested: unknown, path: string) => {
      if (nested === null || typeof nested !== 'object' || Array.isArray(nested)) {
        return;
      }
      const nestedErrors = validatePoint(nested as Point);
      if (nestedErrors !== true) {
        addNestedErrors(errors, path, nestedErrors);
      }
    };

    if (Array.isArray(value.polygons)) {
      value.polygons.forEach((row, rowIndex) => {
        if (Array.isArray(row)) {
          row.forEach((item, index) => validateNested(item, `polygons[${rowIndex}][${index}]`));
        }
      });
    }

  }

  if (Array.isArray(value.polygons)) {
    value.polygons.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `polygons[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `polygons[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  if (value.polygons === null || value.polygons === undefined) {
    addFieldError(errors, "polygons", "required", "required field");
  }

  // A list element is never null: its one error is "required", in place of
  // whatever the element checks above made of it.
  if (Array.isArray(value.polygons)) {
    value.polygons.forEach((row, rowIndex) => {
      if (Array.isArray(row)) {
        row.forEach((item, index) => {
          if (item === null || item === undefined) {
            setFieldErrors(errors, `polygons[${rowIndex}][${index}]`, [{ validator: "required", message: "required field" }]);
          }
        });
      }
    });
  }

  if (Array.isArray(value.weights)) {
    value.weights.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `weights[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `weights[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  // A list element is never null: its one error is "required", in place of
  // whatever the element checks above made of it.
  if (Array.isArray(value.weights)) {
    value.weights.forEach((row, rowIndex) => {
      if (Array.isArray(row)) {
        row.forEach((item, index) => {
          if (item === null || item === undefined) {
            setFieldErrors(errors, `weights[${rowIndex}][${index}]`, [{ validator: "required", message: "required field" }]);
          }
        });
      }
    });
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateGridViewRequired(value: GridView | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateGridView(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const GridViewKnownFields = new Set<string>([
  'id',
  'labels',
  'shades',
  'polygons',
  'weights',
]);

type GridViewTextParser = (value: string) => unknown;

function parseGridViewInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: GridViewTextParser
): GridView {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseGridView ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseGridView ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !GridViewKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseGridView ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as GridView;
  const validationResult = validateGridView(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseGridView ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseGridViewJson(input: string | unknown): GridView {
  return parseGridViewInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseGridViewJsonNonStrict(input: string | unknown): GridView {
  return parseGridViewInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseGridViewYaml(input: string | unknown): GridView {
  return parseGridViewInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseGridViewYamlNonStrict(input: string | unknown): GridView {
  return parseGridViewInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a GridView from JSON wire format into runtime types
 */

export function parseGridViewFromJSON(input: unknown): GridView {
  if (input == null || typeof input !== 'object') {
    return input as GridView;
  }
  const obj = input as Record<string, unknown>;
  return {
    ...(obj as unknown as GridView),

    polygons: Array.isArray(obj.polygons)
      ? (obj.polygons as unknown[]).map((row) =>
          Array.isArray(row) ? row.map((item) => parsePointFromJSON(item)) : (row as Point[])
        )
      : (obj.polygons as Point[][]),

  };
}
