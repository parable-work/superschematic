// Code generated by tsgen. DO NOT EDIT.

import type {
  Board,
  BoardPoint,
} from '../../types';
import { maskSecretsBoardPoint } from './boardpoint';

/**
 * Returns a copy of Board with fields marked @secret cleared.
 */
export function maskSecretsBoard(value: Board): Board {
  return {
    id: value.id,
    labels: Array.isArray(value.labels) ? value.labels : [],
    states: Array.isArray(value.states) ? value.states : [],
    walls: Array.isArray(value.walls) ? value.walls.map((row: BoardPoint[]) => Array.isArray(row) ? row.map((item: BoardPoint) => maskSecretsBoardPoint(item)) : row) : [],
    scores: Array.isArray(value.scores) ? value.scores : null,
  };
}
