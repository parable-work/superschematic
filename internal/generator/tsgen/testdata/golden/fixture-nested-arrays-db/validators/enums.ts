// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from 'superscalar/validation';
import {
  CellState,
} from '../types';

/**
 * Checks if a value is a valid CellState
 */
export function isValidCellState(value: unknown): value is CellState {
  return Object.values(CellState).includes(value as CellState);
}

/**
 * Validates a CellState value (optional field)
 */
export function validateCellState(
  value: CellState | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  if (!isValidCellState(value)) {
    errors.push({ validator: "enum", message: `must be a valid CellState value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a CellState value (required field)
 */
export function validateCellStateRequired(
  value: CellState | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    errors.push({ validator: "required", message: "required field" });
    return [false, errors];
  }

  if (!isValidCellState(value)) {
    errors.push({ validator: "enum", message: `must be a valid CellState value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}
