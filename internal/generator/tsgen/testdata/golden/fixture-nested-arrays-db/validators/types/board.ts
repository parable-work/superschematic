// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
  addNestedErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { load as loadYaml } from 'js-yaml';
import type { Board, BoardPoint } from '../../types';

import { validateIdentityUUIDRequired, validateIdentityUUID } from '../scalars/identity_uuid';

import { validateCellStateRequired, validateCellState } from '../enums';

import { parseBoardPointFromJSON, validateBoardPoint } from './boardpoint';

/**
 * Validates a Board object
 */
export function validateBoard(value: Board | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (value.id !== null && value.id !== undefined) {
    const [valid, fieldErrors] = validateIdentityUUID(value.id);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "id", fieldErrors);
    }
  }

  if (Array.isArray(value.labels)) {
    value.labels.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `labels[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `labels[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  if (value.labels === null || value.labels === undefined) {
    addFieldError(errors, "labels", "required", "required field");
  }

  if (Array.isArray(value.states)) {
    value.states.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `states[${rowIndex}]`, "required", "required field");
        return;
      }
      if (!Array.isArray(row)) {
        addFieldError(errors, `states[${rowIndex}]`, "type", "expected an array");
        return;
      }
      row.forEach((item, index) => {
        const [valid, fieldErrors] = validateCellStateRequired(item);
        if (!valid && fieldErrors) {
          setFieldErrors(errors, `states[${rowIndex}][${index}]`, fieldErrors);
        }
      });
    });
  } else {
    addFieldError(errors, "states", "required", "required field");
  }

  {
    // A nested object is validated as its own type, its errors under the
    // field's path; any other value is left to the field's other checks.
    const validateNested = (nested: unknown, path: string) => {
      if (nested === null || typeof nested !== 'object' || Array.isArray(nested)) {
        return;
      }
      const nestedErrors = validateBoardPoint(nested as BoardPoint);
      if (nestedErrors !== true) {
        addNestedErrors(errors, path, nestedErrors);
      }
    };

    if (Array.isArray(value.walls)) {
      value.walls.forEach((row, rowIndex) => {
        if (Array.isArray(row)) {
          row.forEach((item, index) => validateNested(item, `walls[${rowIndex}][${index}]`));
        }
      });
    }

  }

  if (Array.isArray(value.walls)) {
    value.walls.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `walls[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `walls[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  if (value.walls === null || value.walls === undefined) {
    addFieldError(errors, "walls", "required", "required field");
  }

  if (Array.isArray(value.scores)) {
    value.scores.forEach((row, rowIndex) => {
      if (row === null || row === undefined) {
        addFieldError(errors, `scores[${rowIndex}]`, "required", "required field");
      } else if (!Array.isArray(row)) {
        addFieldError(errors, `scores[${rowIndex}]`, "type", "expected an array");
      }
    });
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateBoardRequired(value: Board | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateBoard(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const BoardKnownFields = new Set<string>([
  'id',
  'labels',
  'states',
  'walls',
  'scores',
]);

type BoardTextParser = (value: string) => unknown;

function parseBoardInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: BoardTextParser
): Board {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseBoard ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseBoard ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !BoardKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseBoard ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as Board;
  const validationResult = validateBoard(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseBoard ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseBoardJson(input: string | unknown): Board {
  return parseBoardInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseBoardJsonNonStrict(input: string | unknown): Board {
  return parseBoardInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseBoardYaml(input: string | unknown): Board {
  return parseBoardInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseBoardYamlNonStrict(input: string | unknown): Board {
  return parseBoardInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a Board from JSON wire format into runtime types
 */

export function parseBoardFromJSON(input: unknown): Board {
  if (input == null || typeof input !== 'object') {
    return input as Board;
  }
  const obj = input as Record<string, unknown>;
  return {
    ...(obj as unknown as Board),

    walls: Array.isArray(obj.walls)
      ? (obj.walls as unknown[]).map((row) =>
          Array.isArray(row) ? row.map((item) => parseBoardPointFromJSON(item)) : (row as BoardPoint[])
        )
      : (obj.walls as BoardPoint[][]),

  };
}
