// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { expectNumber } from '../primitives';
import { load as loadYaml } from 'js-yaml';
import type { BoardPoint } from '../../types';

/**
 * Validates a BoardPoint object
 */
export function validateBoardPoint(value: BoardPoint | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (value.x === null || value.x === undefined) {
    addFieldError(errors, "x", "required", "required field");
  }

  expectNumber(errors, "x", value.x);

  if (value.y === null || value.y === undefined) {
    addFieldError(errors, "y", "required", "required field");
  }

  expectNumber(errors, "y", value.y);

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateBoardPointRequired(value: BoardPoint | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateBoardPoint(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const BoardPointKnownFields = new Set<string>([
  'x',
  'y',
]);

type BoardPointTextParser = (value: string) => unknown;

function parseBoardPointInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: BoardPointTextParser
): BoardPoint {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseBoardPoint ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseBoardPoint ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !BoardPointKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseBoardPoint ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as BoardPoint;
  const validationResult = validateBoardPoint(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseBoardPoint ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseBoardPointJson(input: string | unknown): BoardPoint {
  return parseBoardPointInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseBoardPointJsonNonStrict(input: string | unknown): BoardPoint {
  return parseBoardPointInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseBoardPointYaml(input: string | unknown): BoardPoint {
  return parseBoardPointInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseBoardPointYamlNonStrict(input: string | unknown): BoardPoint {
  return parseBoardPointInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a BoardPoint from JSON wire format into runtime types
 */

export function parseBoardPointFromJSON(input: unknown): BoardPoint {
  return input as BoardPoint;
}
