// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from '@psgen/scalar-lib/validation';
import type { ScalarValidationResult, ValidationResult } from '@psgen/scalar-lib/validation';
import { load as loadYaml } from 'js-yaml';
import type { CreateTenantInput } from '../../types';

import { validateIdentityNameRequired, validateIdentityName } from '../scalars/identity_name';

import { validateIdentitySlugRequired, validateIdentitySlug } from '../scalars/identity_slug';

/**
 * Validates a CreateTenantInput object
 */
export function validateCreateTenantInput(value: CreateTenantInput | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateIdentityNameRequired(value.name);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "name", fieldErrors);
    }
  }

  {
    const [valid, fieldErrors] = validateIdentitySlugRequired(value.slug);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "slug", fieldErrors);
    }
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateCreateTenantInputRequired(value: CreateTenantInput | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateCreateTenantInput(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const CreateTenantInputKnownFields = new Set<string>([
  'name',
  'slug',
]);

type CreateTenantInputTextParser = (value: string) => unknown;

function parseCreateTenantInputInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: CreateTenantInputTextParser
): CreateTenantInput {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseCreateTenantInput ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseCreateTenantInput ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !CreateTenantInputKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseCreateTenantInput ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as CreateTenantInput;
  const validationResult = validateCreateTenantInput(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseCreateTenantInput ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseCreateTenantInputJson(input: string | unknown): CreateTenantInput {
  return parseCreateTenantInputInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseCreateTenantInputJsonNonStrict(input: string | unknown): CreateTenantInput {
  return parseCreateTenantInputInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseCreateTenantInputYaml(input: string | unknown): CreateTenantInput {
  return parseCreateTenantInputInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseCreateTenantInputYamlNonStrict(input: string | unknown): CreateTenantInput {
  return parseCreateTenantInputInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a CreateTenantInput from JSON wire format into runtime types
 */

export function parseCreateTenantInputFromJSON(input: unknown): CreateTenantInput {
  return input as CreateTenantInput;
}
