// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from '@psgen/scalar-lib/validation';
import type { ScalarValidationResult, ValidationResult } from '@psgen/scalar-lib/validation';
import { load as loadYaml } from 'js-yaml';
import type { TenantView } from '../../types';

import { validateIdentityUUIDRequired, validateIdentityUUID } from '../scalars/identity_uuid';

import { validateIdentityNameRequired, validateIdentityName } from '../scalars/identity_name';

/**
 * Validates a TenantView object
 */
export function validateTenantView(value: TenantView | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  {
    const [valid, fieldErrors] = validateIdentityUUIDRequired(value.id);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "id", fieldErrors);
    }
  }

  {
    const [valid, fieldErrors] = validateIdentityNameRequired(value.name);
    if (!valid && fieldErrors) {
      setFieldErrors(errors, "name", fieldErrors);
    }
  }

  if (value.userCount === null || value.userCount === undefined) {
    addFieldError(errors, "userCount", "required", "required field");
  }

  if (value.internalDebugLabel === null || value.internalDebugLabel === undefined) {
    addFieldError(errors, "internalDebugLabel", "required", "required field");
  }

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validateTenantViewRequired(value: TenantView | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validateTenantView(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const TenantViewKnownFields = new Set<string>([
  'id',
  'name',
  'userCount',
  'internalDebugLabel',
]);

type TenantViewTextParser = (value: string) => unknown;

function parseTenantViewInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: TenantViewTextParser
): TenantView {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parseTenantView ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parseTenantView ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !TenantViewKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parseTenantView ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as TenantView;
  const validationResult = validateTenantView(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parseTenantView ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parseTenantViewJson(input: string | unknown): TenantView {
  return parseTenantViewInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantViewJsonNonStrict(input: string | unknown): TenantView {
  return parseTenantViewInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parseTenantViewYaml(input: string | unknown): TenantView {
  return parseTenantViewInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parseTenantViewYamlNonStrict(input: string | unknown): TenantView {
  return parseTenantViewInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a TenantView from JSON wire format into runtime types
 */

export function parseTenantViewFromJSON(input: unknown): TenantView {
  return input as TenantView;
}
