// Code generated by tsgen. DO NOT EDIT.

import type { ScalarValidationResult, ValidationError } from 'superscalar/validation';
import {
  TenantListStatus,
} from '../types';

/**
 * Checks if a value is a valid TenantListStatus
 */
export function isValidTenantListStatus(value: unknown): value is TenantListStatus {
  return Object.values(TenantListStatus).includes(value as TenantListStatus);
}

/**
 * Validates a TenantListStatus value (optional field)
 */
export function validateTenantListStatus(
  value: TenantListStatus | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    return [true, null];
  }

  if (!isValidTenantListStatus(value)) {
    errors.push({ validator: "enum", message: `must be a valid TenantListStatus value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}

/**
 * Validates a TenantListStatus value (required field)
 */
export function validateTenantListStatusRequired(
  value: TenantListStatus | string | null | undefined
): ScalarValidationResult {
  const errors: ValidationError[] = [];

  if (value === null || value === undefined) {
    errors.push({ validator: "required", message: "required field" });
    return [false, errors];
  }

  if (!isValidTenantListStatus(value)) {
    errors.push({ validator: "enum", message: `must be a valid TenantListStatus value` });
  }

  return errors.length > 0 ? [false, errors] : [true, null];
}
