// Code generated by tsgen. DO NOT EDIT.

import {
  newValidationErrors,
  addFieldError,
  setFieldErrors,
} from 'superscalar/validation';
import type { ScalarValidationResult, ValidationResult } from 'superscalar/validation';
import { expectNumber } from '../primitives';
import { load as loadYaml } from 'js-yaml';
import type { Point } from '../../types';

/**
 * Validates a Point object
 */
export function validatePoint(value: Point | null | undefined): ValidationResult {
  const errors = newValidationErrors();

  if (value === null || value === undefined) {
    return true;
  }

  if (value.x === null || value.x === undefined) {
    addFieldError(errors, "x", "required", "required field");
  }

  expectNumber(errors, "x", value.x);

  if (value.y === null || value.y === undefined) {
    addFieldError(errors, "y", "required", "required field");
  }

  expectNumber(errors, "y", value.y);

  return Object.keys(errors).length > 0 ? errors : true;
}

export function validatePointRequired(value: Point | null | undefined): ScalarValidationResult {
  if (value === null || value === undefined) {
    return [false, [{ validator: 'required', message: 'required field' }]];
  }

  const validationResult = validatePoint(value);
  if (validationResult !== true) {
    return [false, [{ validator: 'object', message: JSON.stringify(validationResult) }]];
  }

  return [true, null];
}

const PointKnownFields = new Set<string>([
  'x',
  'y',
]);

type PointTextParser = (value: string) => unknown;

function parsePointInput(
  input: string | unknown,
  strict: boolean,
  sourceFormat: 'json' | 'yaml',
  parseText: PointTextParser
): Point {
  let parsed: unknown = input;

  if (typeof input === 'string') {
    try {
      parsed = parseText(input);
    } catch (error) {
      const reason = error instanceof Error ? error.message : String(error);
      throw new Error(`parsePoint ${sourceFormat} parse failed: ${reason}`);
    }
  }

  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) {
    throw new Error(`parsePoint ${sourceFormat} expects an object input`);
  }

  const candidate = parsed as Record<string, unknown>;

  // Apply @default values from the schema for keys absent from the payload.

  if (strict) {
    const unknownFields = Object.keys(candidate).filter(
      (fieldName) => !PointKnownFields.has(fieldName)
    );
    if (unknownFields.length > 0) {
      throw new Error(
        `parsePoint ${sourceFormat} contains unknown fields: ${unknownFields.join(', ')}`
      );
    }
  }

  const typedCandidate = candidate as unknown as Point;
  const validationResult = validatePoint(typedCandidate);
  if (validationResult !== true) {
    throw new Error(
      `parsePoint ${sourceFormat} validation failed: ${JSON.stringify(validationResult)}`
    );
  }

  return typedCandidate;
}

export function parsePointJson(input: string | unknown): Point {
  return parsePointInput(input, true, 'json', (value) => JSON.parse(value) as unknown);
}

export function parsePointJsonNonStrict(input: string | unknown): Point {
  return parsePointInput(input, false, 'json', (value) => JSON.parse(value) as unknown);
}

export function parsePointYaml(input: string | unknown): Point {
  return parsePointInput(input, true, 'yaml', (value) => loadYaml(value));
}

export function parsePointYamlNonStrict(input: string | unknown): Point {
  return parsePointInput(input, false, 'yaml', (value) => loadYaml(value));
}

/**
 * Parses a Point from JSON wire format into runtime types
 */

export function parsePointFromJSON(input: unknown): Point {
  return input as Point;
}
