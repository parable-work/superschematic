-- Generated by superschematic from {{ .Owner }} ({{ .TypeName }}). DO NOT EDIT.
-- Regenerate with: superschematic build <service-dir>
-- Projection {{ .Relation }}: a change to the declaration is a new migration
-- stamp; this file is immutable once applied.
SET lock_timeout = '5s';
SET statement_timeout = '5min';

CREATE SCHEMA IF NOT EXISTS {{ .QuotedPool }};
{{- if .QuotedViewOwner }}

-- Created as the owner role so the schema's default privileges for that
-- role (bootstrap migration) grant the read role SELECT on the view. The
-- migration runner must be a member of {{ .ViewOwner }}.
SET ROLE {{ .QuotedViewOwner }};
{{- end }}

DROP VIEW IF EXISTS {{ .QualifiedView }};

{{ projectionViewDDL . }}
{{- if .QuotedViewOwner }}

RESET ROLE;
{{- end }}
