//! Code generated by superschematic rustrestgen. DO NOT EDIT.

use async_trait::async_trait;
use superschematic_http_runtime::{ApiError, RequestContext};
use serde_json::Value;
use std::sync::Arc;
#[async_trait]
pub trait GridImplementation: Send + Sync + 'static {
    /// Store a grid from a request body.
    async fn save_grid(&self, ctx: RequestContext, payload: Value) -> Result<Value, ApiError>;
    async fn get_grid(&self, ctx: RequestContext, payload: Value) -> Result<Value, ApiError>;
    /// One grid's labels as a bare list of lists, at most `limit` rows.
    async fn grid_labels(&self, ctx: RequestContext, payload: Value) -> Result<Value, ApiError>;
    /// Replace a grid's labels; the body argument is a list of lists.
    async fn replace_labels(&self, ctx: RequestContext, payload: Value) -> Result<Value, ApiError>;
}


#[derive(Clone)]
pub struct Implementations {
    pub grid: Arc<dyn GridImplementation>,
}
