# {{.PythonModuleName}}

Generated Python types for the **{{.SchemaName}}** schema.

> **Auto-generated Code**: This package is automatically generated from {{.Naming.SchemaLanguage}} definitions. Do not edit manually. Regenerate with `superschematic build <service-dir>`.

## Overview

This package provides type-safe Python models generated from {{.Naming.SchemaLanguage}} definitions using Pydantic v2. All types include:

- **Runtime validation** with detailed error messages
- **Type hints** for IDE autocomplete and static type checking
- **JSON / YAML serialization and deserialization**
{{if .NeedsScalarLib -}}
- **Custom scalar validation** from the scalar library
{{end -}}
- **Field-level constraints** (min/max length, patterns, ranges)

## Installation

```bash
uv pip install {{.PythonModuleName}}
```

## Dependencies

- Python >= 3.12
- Pydantic >= 2.12.0
{{if .NeedsYAML}}- PyYAML >= 6.0.0
{{end -}}
{{if .NeedsScalarLib}}- {{.Naming.ScalarPyPIDist}} >= 1.0.0
{{end}}
{{range .DependencyModules}}- {{.}} >= 1.0.0
{{end -}}
## Usage

```python
from {{.PythonModuleName}} import *

# Parse and validate payloads
obj = SomeType.from_json(payload)

# Serialize back out
json_text = obj.to_json()

# Collect all validation errors at once
errors = obj.validate_all()
if errors:
    print(errors.to_dict())
```
{{if gt (len .Scalars) 0}}
## Scalar Types

This package includes custom scalar types with built-in validation:

{{range .Scalars}}- `{{pythonScalarSymbol .}}`{{if ne (pythonScalarSymbol .) .Name}} (canonical: `{{.Name}}`){{end}}{{if scalarDoc .}}: {{scalarDoc .}}{{end}}
{{end}}{{end}}
{{if gt (len .Enums) 0}}
## Enums

{{range .Enums}}- `{{.Name}}`
{{end}}{{end}}
---

Generated at {{.Timestamp}}.
