# Code generated by pygen. DO NOT EDIT.

"""
Complex types for fixture-general schema.

This module defines Pydantic models for all complex types, providing
validation, serialization, and type safety.
"""

from __future__ import annotations

from typing import Optional, List, Any, Dict, TYPE_CHECKING
import json
import re
from pydantic import BaseModel, ConfigDict, Field, TypeAdapter, ValidationError as PydanticValidationError

from .scalars import (
    NetworkUrl
)

from .enums import (
    FixtureEnvironment
)

from .validation_errors import ValidationErrors

def _safe_load_yaml(input_data: str | bytes) -> Dict[str, Any]:
    """
    Load YAML content while keeping module import independent of PyYAML.
    """
    try:
        import yaml
    except ImportError as e:
        raise ImportError(
            "PyYAML is required for YAML parsing helpers. Install PyYAML>=6.0.0."
        ) from e

    parsed_data = yaml.safe_load(input_data)
    if parsed_data is None:
        return {}
    if not isinstance(parsed_data, dict):
        raise ValueError("YAML content must decode to an object")
    return parsed_data

class FixtureConfig(BaseModel):
    """
    FixtureConfig type
    """

    model_config = ConfigDict(
        strict=True,
        validate_assignment=True,
        extra='ignore',
        use_enum_values=True,
        populate_by_name=True,
    )

    database_url: NetworkUrl = Field(..., alias="DATABASE_URL", serialization_alias="DATABASE_URL")

    jwt_secret: str = Field(..., alias="JWT_SECRET", serialization_alias="JWT_SECRET")

    port: float = Field(default=8080, alias="PORT", serialization_alias="PORT")

    environment: FixtureEnvironment = Field(default="development", alias="ENVIRONMENT", serialization_alias="ENVIRONMENT")

    def validate_all(self) -> ValidationErrors:
        """
        Perform comprehensive validation and return all errors.

        Returns:
            ValidationErrors object containing any validation errors.
            If no errors, the errors dict will be empty.
        """
        errors = ValidationErrors()

        # Validate DATABASE_URL
        if self.database_url is None:
            errors.add_field_error("database_url", "required", "required field")
        else:

            if len(str(self.database_url)) > 2048:
                errors.add_field_error("database_url", "maxLength", "must be at most 2048 characters")
            if re.search(r"^https?://[\w\-\{\}]+(\.[\w\-\{\}]+)+([:/?#][\w\-\._~:/?#\[\]@!\$&'\(\)\*\+,;=\{\}%]*)?$", str(self.database_url)) is None:
                errors.add_field_error("database_url", "pattern", "invalid format")

        # Validate JWT_SECRET
        if self.jwt_secret is None:
            errors.add_field_error("jwt_secret", "required", "required field")

        # Validate PORT
        if self.port is None:
            errors.add_field_error("port", "required", "required field")

        # Validate ENVIRONMENT
        if self.environment is None:
            errors.add_field_error("environment", "required", "required field")

        return errors

    def to_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a dictionary.

        Returns:
            Dictionary representation of the model.
        """
        return self.model_dump(mode='python', exclude_none=False)

    def to_json_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a JSON-serializable dictionary.

        Returns:
            JSON-serializable dictionary representation.
        """
        return self.model_dump(mode='json', exclude_none=False)

    def to_json(self) -> str:
        """
        Convert the model to a stable JSON string.

        Returns:
            JSON string representation.
        """
        return self.model_dump_json(exclude_none=False)

    @classmethod
    def from_dict(cls, input_data: Dict[str, Any]) -> "FixtureConfig":
        """
        Parse a model from a dictionary using strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=True)

    @classmethod
    def from_dict_non_strict(cls, input_data: Dict[str, Any]) -> "FixtureConfig":
        """
        Parse a model from a dictionary using non-strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=False)

    @classmethod
    def from_json(cls, input_data: str | bytes) -> "FixtureConfig":
        """
        Parse a model from JSON text/bytes using strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict(parsed_data)

    @classmethod
    def from_json_non_strict(cls, input_data: str | bytes) -> "FixtureConfig":
        """
        Parse a model from JSON text/bytes using non-strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict_non_strict(parsed_data)

    @classmethod
    def from_yaml(cls, input_data: str | bytes) -> "FixtureConfig":
        """
        Parse a model from YAML text/bytes using strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict(parsed_data)

    @classmethod
    def from_yaml_non_strict(cls, input_data: str | bytes) -> "FixtureConfig":
        """
        Parse a model from YAML text/bytes using non-strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict_non_strict(parsed_data)

    def mask_secrets(self) -> "FixtureConfig":
        """
        Return a copy of this object with secret fields masked.
        """
        return self.model_copy(update={
            "jwt_secret": "",
        })

class FixtureFilter(BaseModel):
    """
    JSON-persisted filter: exercises optional-list constraint gating.
# Absent/null must skip listMin; an explicit [] must fail it (decode must
# not normalize an absent optional list into an empty non-nil slice).
    """

    model_config = ConfigDict(
        strict=True,
        validate_assignment=True,
        extra='ignore',
        use_enum_values=True,
        populate_by_name=True,
    )

    kind: str = Field(..., alias="kind", serialization_alias="kind")

    values: Optional[List[str]] = Field(default=None, alias="values", serialization_alias="values")

    def validate_all(self) -> ValidationErrors:
        """
        Perform comprehensive validation and return all errors.

        Returns:
            ValidationErrors object containing any validation errors.
            If no errors, the errors dict will be empty.
        """
        errors = ValidationErrors()

        # Validate kind
        if self.kind is None:
            errors.add_field_error("kind", "required", "required field")
        else:

            if len(str(self.kind)) > 32:
                errors.add_field_error("kind", "maxLength", "must be at most 32 characters")

        # Validate values
        if self.values is not None:
            # A None entry is reported at its own index below; the whole-value
            # check would repeat it at the field.
            if not (isinstance(self.values, list) and None in self.values):
                try:
                    TypeAdapter(List[str]).validate_python(self.values)
                except PydanticValidationError as e:
                    errors.add_field_error("values", "invalid", str(e))

            if isinstance(self.values, list):
                for index, item in enumerate(self.values):
                    if item is None:
                        errors.add_field_error(f"values[{index}]", "required", "required field")
            if isinstance(self.values, list):
                for index, item in enumerate(self.values):
                    if item is not None and len(str(item)) > 64:
                        errors.add_field_error(f"values[{index}]", "maxLength", "must be at most 64 characters")
            if isinstance(self.values, list) and len(self.values) > 10:
                errors.add_field_error("values", "listMax", "must contain at most 10 items")
            if isinstance(self.values, list) and len(self.values) < 1:
                errors.add_field_error("values", "listMin", "must contain at least 1 items")

        return errors

    def to_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a dictionary.

        Returns:
            Dictionary representation of the model.
        """
        return self.model_dump(mode='python', exclude_none=False)

    def to_json_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a JSON-serializable dictionary.

        Returns:
            JSON-serializable dictionary representation.
        """
        return self.model_dump(mode='json', exclude_none=False)

    def to_json(self) -> str:
        """
        Convert the model to a stable JSON string.

        Returns:
            JSON string representation.
        """
        return self.model_dump_json(exclude_none=False)

    @classmethod
    def from_dict(cls, input_data: Dict[str, Any]) -> "FixtureFilter":
        """
        Parse a model from a dictionary using strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=True)

    @classmethod
    def from_dict_non_strict(cls, input_data: Dict[str, Any]) -> "FixtureFilter":
        """
        Parse a model from a dictionary using non-strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=False)

    @classmethod
    def from_json(cls, input_data: str | bytes) -> "FixtureFilter":
        """
        Parse a model from JSON text/bytes using strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict(parsed_data)

    @classmethod
    def from_json_non_strict(cls, input_data: str | bytes) -> "FixtureFilter":
        """
        Parse a model from JSON text/bytes using non-strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict_non_strict(parsed_data)

    @classmethod
    def from_yaml(cls, input_data: str | bytes) -> "FixtureFilter":
        """
        Parse a model from YAML text/bytes using strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict(parsed_data)

    @classmethod
    def from_yaml_non_strict(cls, input_data: str | bytes) -> "FixtureFilter":
        """
        Parse a model from YAML text/bytes using non-strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict_non_strict(parsed_data)

    def mask_secrets(self) -> "FixtureFilter":
        """
        Return a copy of this object with secret fields masked.
        """
        return self.model_copy(update={
        })

class RetryPolicy(BaseModel):
    """
    RetryPolicy type
    """

    model_config = ConfigDict(
        strict=True,
        validate_assignment=True,
        extra='ignore',
        use_enum_values=True,
        populate_by_name=True,
    )

    max_attempts: float = Field(..., alias="maxAttempts", serialization_alias="maxAttempts")

    backoff_seconds: Optional[float] = Field(default=None, alias="backoffSeconds", serialization_alias="backoffSeconds")

    def validate_all(self) -> ValidationErrors:
        """
        Perform comprehensive validation and return all errors.

        Returns:
            ValidationErrors object containing any validation errors.
            If no errors, the errors dict will be empty.
        """
        errors = ValidationErrors()

        # Validate maxAttempts
        if self.max_attempts is None:
            errors.add_field_error("max_attempts", "required", "required field")

        # Validate backoffSeconds
        if self.backoff_seconds is not None:
            try:
                TypeAdapter(float).validate_python(self.backoff_seconds)
            except PydanticValidationError as e:
                errors.add_field_error("backoff_seconds", "invalid", str(e))

        return errors

    def to_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a dictionary.

        Returns:
            Dictionary representation of the model.
        """
        return self.model_dump(mode='python', exclude_none=False)

    def to_json_dict(self) -> Dict[str, Any]:
        """
        Convert the model to a JSON-serializable dictionary.

        Returns:
            JSON-serializable dictionary representation.
        """
        return self.model_dump(mode='json', exclude_none=False)

    def to_json(self) -> str:
        """
        Convert the model to a stable JSON string.

        Returns:
            JSON string representation.
        """
        return self.model_dump_json(exclude_none=False)

    @classmethod
    def from_dict(cls, input_data: Dict[str, Any]) -> "RetryPolicy":
        """
        Parse a model from a dictionary using strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=True)

    @classmethod
    def from_dict_non_strict(cls, input_data: Dict[str, Any]) -> "RetryPolicy":
        """
        Parse a model from a dictionary using non-strict validation.

        Args:
            input_data: Dictionary payload to parse.

        Returns:
            Parsed model instance.
        """
        return cls.model_validate(input_data, strict=False)

    @classmethod
    def from_json(cls, input_data: str | bytes) -> "RetryPolicy":
        """
        Parse a model from JSON text/bytes using strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict(parsed_data)

    @classmethod
    def from_json_non_strict(cls, input_data: str | bytes) -> "RetryPolicy":
        """
        Parse a model from JSON text/bytes using non-strict validation.

        Args:
            input_data: JSON payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = json.loads(input_data)
        if not isinstance(parsed_data, dict):
            raise ValueError("JSON content must decode to an object")
        return cls.from_dict_non_strict(parsed_data)

    @classmethod
    def from_yaml(cls, input_data: str | bytes) -> "RetryPolicy":
        """
        Parse a model from YAML text/bytes using strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict(parsed_data)

    @classmethod
    def from_yaml_non_strict(cls, input_data: str | bytes) -> "RetryPolicy":
        """
        Parse a model from YAML text/bytes using non-strict validation.

        Args:
            input_data: YAML payload to parse.

        Returns:
            Parsed model instance.
        """
        parsed_data = _safe_load_yaml(input_data)
        return cls.from_dict_non_strict(parsed_data)

    def mask_secrets(self) -> "RetryPolicy":
        """
        Return a copy of this object with secret fields masked.
        """
        return self.model_copy(update={
        })

def default_fixture_config() -> FixtureConfig:
    """Return a fresh FixtureConfig platform default."""
    return FixtureConfig.model_validate_json("{\"DATABASE_URL\":\"https://example.com\",\"ENVIRONMENT\":\"production\",\"JWT_SECRET\":\"test-secret\",\"PORT\":8080}")
