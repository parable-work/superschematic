// Code generated by superschematic apigen. DO NOT EDIT.
//
// This file defines the implementation interfaces for each namespace.
// Implement these interfaces in your service's internal/route-impl package to provide business logic.

package fixturenestedarraysapi

import (
	"context"
	types "example.com/schemas/types/go/fixture-nested-arrays-api"
	"fmt"
)

// GridImplementation defines the interface for grid namespace handlers.
// Implement in internal/route-impl to provide business logic.
type GridImplementation interface {
	// SaveGrid handles POST /api/grids
	//
	// Store a grid from a request body.
	SaveGrid(ctx context.Context, input *types.SaveGridInput) (*types.GridView, error)
	// GetGrid handles GET /api/grids/{id}
	GetGrid(ctx context.Context, id types.IdentityUUID) (*types.GridView, error)
	// GridLabels handles GET /api/grids/{id}/labels
	//
	// One grid's labels as a bare list of lists, at most `limit` rows.
	GridLabels(ctx context.Context, id types.IdentityUUID, limit *float64) ([][]string, error)
	// ReplaceLabels handles PUT /api/grids/{id}/labels
	//
	// Replace a grid's labels; the body argument is a list of lists.
	ReplaceLabels(ctx context.Context, id types.IdentityUUID, labels [][]string) (*types.GridView, error)
}

// Implementations holds all namespace implementation interfaces.
// Pass this to RegisterRoutes() to wire up your business logic.
type Implementations struct {
	Grid GridImplementation
}

// ValidateImplementations checks that all required implementations are provided.
// Returns an error if any implementation is nil.
func (i *Implementations) ValidateImplementations() error {
	if i.Grid == nil {
		return fmt.Errorf("Implementations.Grid is required")
	}
	return nil
}
