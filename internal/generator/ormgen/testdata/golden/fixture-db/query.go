// Code generated by psgen ormgen. DO NOT EDIT.

package orm

import (
	"fmt"
	"strings"
	"time"

	types "github.com/parable-platform/platform-schemas/types/go/fixture-db"
)

// Filter operation types for different scalar types

// UUIDFilter provides filtering operations for UUID fields
type UUIDFilter struct {
	Eq     *types.IdentityUUID
	In     []types.IdentityUUID
	IsNull *bool // true for IS NULL, false for IS NOT NULL
}

// UserIDFilter is retained for legacy callers; user IDs share UUID filtering.
type UserIDFilter = UUIDFilter

// StringFilter provides filtering operations for string fields
type StringFilter struct {
	Eq *string
	// In matches any value in the set. It is the allowlist primitive for
	// enum-backed columns: a set predicate that runs in SQL rather than a
	// projection the caller has to remember to filter in application code.
	// An empty or nil slice adds no condition, so a caller narrowing a result
	// set must treat "no allowed values" as its own case rather than passing
	// the empty slice and getting every row back.
	In    []string
	ILike *string // Case-insensitive LIKE
}

// Int64Filter provides filtering operations for integer fields
type Int64Filter struct {
	Eq      *int64
	Gt      *int64
	Gte     *int64
	Lt      *int64
	Lte     *int64
	Between *Int64Range
}

// Int64Range represents a range for int64 values
type Int64Range struct {
	Min int64
	Max int64
}

// FloatFilter provides filtering operations for float fields
type FloatFilter struct {
	Eq      *float64
	Gt      *float64
	Gte     *float64
	Lt      *float64
	Lte     *float64
	Between *FloatRange
}

// FloatRange represents a range for float values
type FloatRange struct {
	Min float64
	Max float64
}

// BoolFilter provides filtering operations for boolean fields
type BoolFilter struct {
	Eq *bool
}

// DateTimeFilter provides filtering operations for DateTime fields
type DateTimeFilter struct {
	Eq      *time.Time
	Gt      *time.Time
	Gte     *time.Time
	Lt      *time.Time
	Lte     *time.Time
	Between *DateTimeRange
	IsNull  *bool // true for IS NULL, false for IS NOT NULL
}

// DateTimeRange represents a range for DateTime values
type DateTimeRange struct {
	Start time.Time
	End   time.Time
}

// DateFilter provides filtering operations for Date fields
type DateFilter struct {
	Eq      *string
	Gt      *string
	Gte     *string
	Lt      *string
	Lte     *string
	Between *DateRange
}

// DateRange represents a range for Date values
type DateRange struct {
	Start string
	End   string
}

// ReferencedByFilter probes for rows another table does or does not
// reference. It becomes an EXISTS / NOT EXISTS subquery over the referencing
// table's foreign key, so a filtered page contains only matching rows -- an
// application-side check cannot do that, because it can never page past rows
// it always drops.
type ReferencedByFilter struct {
	// Exists true keeps rows with at least one referencing row; false keeps
	// rows with none.
	Exists bool

	// IncludeDeleted probes soft-deleted referencing rows too. The default
	// matches ordinary reads and counts only live ones; set it when the
	// question is "was this ever referenced", not "is it referenced now".
	IncludeDeleted bool
}

// ArrayStringFilter provides filtering operations for string array fields
type ArrayStringFilter struct {
	Contains []string
	Overlaps []string
	Eq       []string
}

// ========================================
// Type-Safe Field Selectors and Options
// ========================================

// TenantFields specifies which Tenant fields to select
type TenantFields struct {
	CreatedAt bool
	UpdatedAt bool
	Id        bool
	Name      bool
	Slug      bool
	Email     bool
	Status    bool
	IsActive  bool
	SeatCount bool
	Metadata  bool
	Version   bool
	// UsersNested selects fields for related TenantUser records
	UsersNested *TenantUserFields
}

// ToStringSlice converts TenantFields to []string for internal use.
// Fields are returned in OrderedMembers order to match scan order.
func (f TenantFields) ToStringSlice() []string {
	var fields []string
	if f.CreatedAt {
		fields = append(fields, "created_at")
	}
	if f.UpdatedAt {
		fields = append(fields, "updated_at")
	}
	if f.Id {
		fields = append(fields, "id")
	}
	if f.Name {
		fields = append(fields, "name")
	}
	if f.Slug {
		fields = append(fields, "slug")
	}
	if f.Email {
		fields = append(fields, "email")
	}
	if f.Status {
		fields = append(fields, "status")
	}
	if f.IsActive {
		fields = append(fields, "is_active")
	}
	if f.SeatCount {
		fields = append(fields, "seat_count")
	}
	if f.Metadata {
		fields = append(fields, "metadata")
	}
	if f.Version {
		fields = append(fields, "_version")
	}
	return fields
}

// IsZero returns true if no fields are selected
func (f TenantFields) IsZero() bool {
	if len(f.ToStringSlice()) != 0 {
		return false
	}
	if f.UsersNested != nil {
		return false
	}
	return true
}

// All returns a TenantFields with all fields selected
func (TenantFields) All() TenantFields {
	return TenantFields{
		CreatedAt: true,
		UpdatedAt: true,
		Id:        true,
		Name:      true,
		Slug:      true,
		Email:     true,
		Status:    true,
		IsActive:  true,
		SeatCount: true,
		Metadata:  true,
		Version:   true,
	}
}

// Minimal returns commonly needed fields (id and name-like fields)
func (TenantFields) Minimal() TenantFields {
	return TenantFields{
		Id:    true,
		Name:  true,
		Slug:  true,
		Email: true,
	}
}

// WithoutAudit returns all non-audit fields
func (TenantFields) WithoutAudit() TenantFields {
	return TenantFields{
		Id:        true,
		Name:      true,
		Slug:      true,
		Email:     true,
		Status:    true,
		IsActive:  true,
		SeatCount: true,
		Metadata:  true,
		Version:   true,
	}
}

// TenantOrderByField represents a field that can be used for ordering
type TenantOrderByField string

const (
	TenantOrderByCreatedAt TenantOrderByField = "created_at"
	TenantOrderByUpdatedAt TenantOrderByField = "updated_at"
	TenantOrderById        TenantOrderByField = "id"
	TenantOrderByName      TenantOrderByField = "name"
	TenantOrderBySlug      TenantOrderByField = "slug"
	TenantOrderByEmail     TenantOrderByField = "email"
	TenantOrderByStatus    TenantOrderByField = "status"
	TenantOrderByIsActive  TenantOrderByField = "is_active"
	TenantOrderBySeatCount TenantOrderByField = "seat_count"
	TenantOrderByMetadata  TenantOrderByField = "metadata"
	TenantOrderByVersion   TenantOrderByField = "_version"
)

// TenantOrderBy specifies ordering for Tenant queries
type TenantOrderBy struct {
	Field TenantOrderByField
	Desc  bool
}

// TenantGetOptions contains options for Tenant GetOne operations
type TenantGetOptions struct {
	// Fields specifies which fields to select (if zero, selects all fields)
	Fields TenantFields
}

// TenantFindOptions contains options for Tenant Find operations
type TenantFindOptions struct {
	// Fields specifies which fields to select (if zero, selects all fields)
	Fields TenantFields
	// OrderBy specifies sorting order
	OrderBy []TenantOrderBy
	// Limit limits the number of results (default 100)
	Limit int
	// Offset skips the first N results (for pagination)
	Offset int
	// IncludeDeleted includes soft-deleted records in results (default false)
	IncludeDeleted bool
}

// TenantHistoryOptions contains options for Tenant version history reads.
type TenantHistoryOptions struct {
	// Limit limits the number of results (default 100)
	Limit int
	// Offset skips the first N results (for pagination)
	Offset int
}

// TenantUserFields specifies which TenantUser fields to select
type TenantUserFields struct {
	CreatedAt   bool
	UpdatedAt   bool
	Id          bool
	DisplayName bool
	DeletedAt   bool
	DeletedBy   bool
	Version     bool
	// Tenant selects the tenant_id foreign key field
	Tenant bool
	// TenantNested selects fields for the related Tenant
	TenantNested TenantFields
}

// ToStringSlice converts TenantUserFields to []string for internal use.
// Fields are returned in OrderedMembers order to match scan order.
func (f TenantUserFields) ToStringSlice() []string {
	var fields []string
	if f.CreatedAt {
		fields = append(fields, "created_at")
	}
	if f.UpdatedAt {
		fields = append(fields, "updated_at")
	}
	if f.Id {
		fields = append(fields, "id")
	}
	if f.Tenant {
		fields = append(fields, "tenant_id")
	}
	if f.DisplayName {
		fields = append(fields, "display_name")
	}
	if f.DeletedAt {
		fields = append(fields, "deleted_at")
	}
	if f.DeletedBy {
		fields = append(fields, "deleted_by")
	}
	if f.Version {
		fields = append(fields, "_version")
	}
	return fields
}

// IsZero returns true if no fields are selected
func (f TenantUserFields) IsZero() bool {
	if len(f.ToStringSlice()) != 0 {
		return false
	}
	if !f.TenantNested.IsZero() {
		return false
	}
	return true
}

// All returns a TenantUserFields with all fields selected
func (TenantUserFields) All() TenantUserFields {
	return TenantUserFields{
		CreatedAt:   true,
		UpdatedAt:   true,
		Id:          true,
		DisplayName: true,
		DeletedAt:   true,
		DeletedBy:   true,
		Version:     true,
		Tenant:      true,
	}
}

// Minimal returns commonly needed fields (id and name-like fields)
func (TenantUserFields) Minimal() TenantUserFields {
	return TenantUserFields{
		Id: true,
	}
}

// WithoutAudit returns all non-audit fields
func (TenantUserFields) WithoutAudit() TenantUserFields {
	return TenantUserFields{
		Id:          true,
		DisplayName: true,
		Version:     true,
	}
}

// TenantUserOrderByField represents a field that can be used for ordering
type TenantUserOrderByField string

const (
	TenantUserOrderByCreatedAt   TenantUserOrderByField = "created_at"
	TenantUserOrderByUpdatedAt   TenantUserOrderByField = "updated_at"
	TenantUserOrderById          TenantUserOrderByField = "id"
	TenantUserOrderByDisplayName TenantUserOrderByField = "display_name"
	TenantUserOrderByDeletedAt   TenantUserOrderByField = "deleted_at"
	TenantUserOrderByDeletedBy   TenantUserOrderByField = "deleted_by"
	TenantUserOrderByVersion     TenantUserOrderByField = "_version"
)

// TenantUserOrderBy specifies ordering for TenantUser queries
type TenantUserOrderBy struct {
	Field TenantUserOrderByField
	Desc  bool
}

// TenantUserGetOptions contains options for TenantUser GetOne operations
type TenantUserGetOptions struct {
	// Fields specifies which fields to select (if zero, selects all fields)
	Fields TenantUserFields
}

// TenantUserFindOptions contains options for TenantUser Find operations
type TenantUserFindOptions struct {
	// Fields specifies which fields to select (if zero, selects all fields)
	Fields TenantUserFields
	// OrderBy specifies sorting order
	OrderBy []TenantUserOrderBy
	// Limit limits the number of results (default 100)
	Limit int
	// Offset skips the first N results (for pagination)
	Offset int
	// IncludeDeleted includes soft-deleted records in results (default false)
	IncludeDeleted bool
}

// TenantUserHistoryOptions contains options for TenantUser version history reads.
type TenantUserHistoryOptions struct {
	// Limit limits the number of results (default 100)
	Limit int
	// Offset skips the first N results (for pagination)
	Offset int
}

// ========================================
// Filters and Update Structs
// ========================================

// TenantFilter provides filtering for Tenant queries
type TenantFilter struct {
	CreatedAt *DateTimeFilter
	UpdatedAt *DateTimeFilter
	Id        *UUIDFilter
	Name      *StringFilter
	Slug      *StringFilter
	Email     *StringFilter
	Status    *StringFilter
	IsActive  *BoolFilter
	SeatCount *FloatFilter
	Metadata  *StringFilter
	Version   *Int64Filter
	// ReferencedByTenantUserTenant filters on the presence or absence of a
	// TenantUser whose tenant points at this row.
	ReferencedByTenantUserTenant *ReferencedByFilter
}

// TenantUpdate provides fields that can be updated
type TenantUpdate struct {
	Name      *types.IdentityName
	Slug      *types.IdentitySlug
	Email     *string
	Status    *types.TenantStatus
	IsActive  *bool
	SeatCount *float64
	Metadata  *types.GenericJSON
	// UpdatedBy is automatically set if not provided and user is in context
	UpdatedBy *types.IdentityUUID
}

// NewTenantSnapshotUpdate maps every mutable field from a complete
// Tenant value. It is intended for source-of-truth replacement flows;
// nullable zero values clear their columns instead of leaving stale data.
func NewTenantSnapshotUpdate(input *types.Tenant) *TenantUpdate {
	update := &TenantUpdate{}
	if input == nil {
		return update
	}
	update.Name = &input.Name
	update.Slug = &input.Slug
	update.Email = &input.Email
	update.Status = &input.Status
	update.IsActive = &input.IsActive
	update.SeatCount = &input.SeatCount
	update.Metadata = &input.Metadata
	return update
}

// buildWhereClause builds a WHERE clause from a TenantFilter
func (f *TenantFilter) buildWhereClause(args *[]interface{}, paramOffset int) string {
	if f == nil {
		return ""
	}

	var conditions []string
	paramNum := paramOffset
	if f.CreatedAt != nil {
		if f.CreatedAt.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at = $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Eq)
		}
		if f.CreatedAt.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at > $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Gt)
		}
		if f.CreatedAt.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at >= $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Gte)
		}
		if f.CreatedAt.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at < $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Lt)
		}
		if f.CreatedAt.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at <= $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Lte)
		}
		if f.CreatedAt.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at >= $%d`, paramNum))
			*args = append(*args, f.CreatedAt.Between.Start)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.created_at <= $%d`, paramNum))
			*args = append(*args, f.CreatedAt.Between.End)
		}
		if f.CreatedAt.IsNull != nil {
			if *f.CreatedAt.IsNull {
				conditions = append(conditions, `tenant.created_at IS NULL`)
			} else {
				conditions = append(conditions, `tenant.created_at IS NOT NULL`)
			}
		}
	}
	if f.UpdatedAt != nil {
		if f.UpdatedAt.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at = $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Eq)
		}
		if f.UpdatedAt.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at > $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Gt)
		}
		if f.UpdatedAt.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at >= $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Gte)
		}
		if f.UpdatedAt.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at < $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Lt)
		}
		if f.UpdatedAt.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at <= $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Lte)
		}
		if f.UpdatedAt.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at >= $%d`, paramNum))
			*args = append(*args, f.UpdatedAt.Between.Start)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.updated_at <= $%d`, paramNum))
			*args = append(*args, f.UpdatedAt.Between.End)
		}
		if f.UpdatedAt.IsNull != nil {
			if *f.UpdatedAt.IsNull {
				conditions = append(conditions, `tenant.updated_at IS NULL`)
			} else {
				conditions = append(conditions, `tenant.updated_at IS NOT NULL`)
			}
		}
	}
	if f.Id != nil {
		if f.Id.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.id = $%d`, paramNum))
			*args = append(*args, f.Id.Eq.ToUUID())
		}
		if len(f.Id.In) > 0 {
			placeholders := make([]string, len(f.Id.In))
			for i, id := range f.Id.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, id.ToUUID())
			}
			conditions = append(conditions, fmt.Sprintf(`tenant.id IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Id.IsNull != nil {
			if *f.Id.IsNull {
				conditions = append(conditions, `tenant.id IS NULL`)
			} else {
				conditions = append(conditions, `tenant.id IS NOT NULL`)
			}
		}
	}
	if f.Name != nil {
		if f.Name.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant."name" = $%d`, paramNum))
			*args = append(*args, *f.Name.Eq)
		}
		if len(f.Name.In) > 0 {
			placeholders := make([]string, len(f.Name.In))
			for i, value := range f.Name.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant."name" IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Name.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant."name" ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.Name.ILike+"%")
		}
	}
	if f.Slug != nil {
		if f.Slug.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.slug = $%d`, paramNum))
			*args = append(*args, *f.Slug.Eq)
		}
		if len(f.Slug.In) > 0 {
			placeholders := make([]string, len(f.Slug.In))
			for i, value := range f.Slug.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant.slug IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Slug.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.slug ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.Slug.ILike+"%")
		}
	}
	if f.Email != nil {
		if f.Email.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.email = $%d`, paramNum))
			*args = append(*args, *f.Email.Eq)
		}
		if len(f.Email.In) > 0 {
			placeholders := make([]string, len(f.Email.In))
			for i, value := range f.Email.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant.email IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Email.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.email ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.Email.ILike+"%")
		}
	}
	if f.Status != nil {
		if f.Status.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.status = $%d`, paramNum))
			*args = append(*args, *f.Status.Eq)
		}
		if len(f.Status.In) > 0 {
			placeholders := make([]string, len(f.Status.In))
			for i, value := range f.Status.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant.status IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Status.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.status ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.Status.ILike+"%")
		}
	}
	if f.IsActive != nil && f.IsActive.Eq != nil {
		paramNum++
		conditions = append(conditions, fmt.Sprintf(`tenant.is_active = $%d`, paramNum))
		*args = append(*args, *f.IsActive.Eq)
	}
	if f.SeatCount != nil {
		if f.SeatCount.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count = $%d`, paramNum))
			*args = append(*args, *f.SeatCount.Eq)
		}
		if f.SeatCount.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count > $%d`, paramNum))
			*args = append(*args, *f.SeatCount.Gt)
		}
		if f.SeatCount.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count >= $%d`, paramNum))
			*args = append(*args, *f.SeatCount.Gte)
		}
		if f.SeatCount.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count < $%d`, paramNum))
			*args = append(*args, *f.SeatCount.Lt)
		}
		if f.SeatCount.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count <= $%d`, paramNum))
			*args = append(*args, *f.SeatCount.Lte)
		}
		if f.SeatCount.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count >= $%d`, paramNum))
			*args = append(*args, f.SeatCount.Between.Min)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.seat_count <= $%d`, paramNum))
			*args = append(*args, f.SeatCount.Between.Max)
		}
	}
	if f.Metadata != nil {
		if f.Metadata.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.metadata = $%d`, paramNum))
			*args = append(*args, *f.Metadata.Eq)
		}
		if len(f.Metadata.In) > 0 {
			placeholders := make([]string, len(f.Metadata.In))
			for i, value := range f.Metadata.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant.metadata IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Metadata.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant.metadata ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.Metadata.ILike+"%")
		}
	}
	if f.Version != nil {
		if f.Version.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version = $%d`, paramNum))
			*args = append(*args, *f.Version.Eq)
		}
		if f.Version.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version > $%d`, paramNum))
			*args = append(*args, *f.Version.Gt)
		}
		if f.Version.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version >= $%d`, paramNum))
			*args = append(*args, *f.Version.Gte)
		}
		if f.Version.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version < $%d`, paramNum))
			*args = append(*args, *f.Version.Lt)
		}
		if f.Version.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version <= $%d`, paramNum))
			*args = append(*args, *f.Version.Lte)
		}
		if f.Version.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version >= $%d`, paramNum))
			*args = append(*args, f.Version.Between.Min)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant._version <= $%d`, paramNum))
			*args = append(*args, f.Version.Between.Max)
		}
	}
	if f.ReferencedByTenantUserTenant != nil {
		probe := `SELECT 1 FROM tenant_user WHERE tenant_user.tenant_id = tenant.id`
		if !f.ReferencedByTenantUserTenant.IncludeDeleted {
			probe += ` AND tenant_user.deleted_at IS NULL`
		}
		if f.ReferencedByTenantUserTenant.Exists {
			conditions = append(conditions, `EXISTS (`+probe+`)`)
		} else {
			conditions = append(conditions, `NOT EXISTS (`+probe+`)`)
		}
	}

	if len(conditions) == 0 {
		return ""
	}

	return " WHERE " + strings.Join(conditions, " AND ")
}

// TenantUserFilter provides filtering for TenantUser queries
type TenantUserFilter struct {
	CreatedAt   *DateTimeFilter
	UpdatedAt   *DateTimeFilter
	Id          *UUIDFilter
	DisplayName *StringFilter
	Version     *Int64Filter
	// TenantID allows filtering by foreign key
	TenantID *UUIDFilter
}

// TenantUserUpdate provides fields that can be updated
type TenantUserUpdate struct {
	DisplayName        *string
	DisplayNameSetNull bool
	// TenantID allows updating the foreign key for tenant
	TenantID *types.IdentityUUID
	// UpdatedBy is automatically set if not provided and user is in context
	UpdatedBy *types.IdentityUUID
}

// NewTenantUserSnapshotUpdate maps every mutable field from a complete
// TenantUser value. It is intended for source-of-truth replacement flows;
// nullable zero values clear their columns instead of leaving stale data.
func NewTenantUserSnapshotUpdate(input *types.TenantUser) *TenantUserUpdate {
	update := &TenantUserUpdate{}
	if input == nil {
		return update
	}
	var zeroDisplayName string
	if input.DisplayName == zeroDisplayName {
		update.DisplayNameSetNull = true
	} else {
		update.DisplayName = &input.DisplayName
	}
	return update
}

// buildWhereClause builds a WHERE clause from a TenantUserFilter
func (f *TenantUserFilter) buildWhereClause(args *[]interface{}, paramOffset int) string {
	if f == nil {
		return ""
	}

	var conditions []string
	paramNum := paramOffset
	if f.CreatedAt != nil {
		if f.CreatedAt.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at = $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Eq)
		}
		if f.CreatedAt.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at > $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Gt)
		}
		if f.CreatedAt.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at >= $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Gte)
		}
		if f.CreatedAt.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at < $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Lt)
		}
		if f.CreatedAt.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at <= $%d`, paramNum))
			*args = append(*args, *f.CreatedAt.Lte)
		}
		if f.CreatedAt.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at >= $%d`, paramNum))
			*args = append(*args, f.CreatedAt.Between.Start)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.created_at <= $%d`, paramNum))
			*args = append(*args, f.CreatedAt.Between.End)
		}
		if f.CreatedAt.IsNull != nil {
			if *f.CreatedAt.IsNull {
				conditions = append(conditions, `tenant_user.created_at IS NULL`)
			} else {
				conditions = append(conditions, `tenant_user.created_at IS NOT NULL`)
			}
		}
	}
	if f.UpdatedAt != nil {
		if f.UpdatedAt.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at = $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Eq)
		}
		if f.UpdatedAt.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at > $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Gt)
		}
		if f.UpdatedAt.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at >= $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Gte)
		}
		if f.UpdatedAt.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at < $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Lt)
		}
		if f.UpdatedAt.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at <= $%d`, paramNum))
			*args = append(*args, *f.UpdatedAt.Lte)
		}
		if f.UpdatedAt.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at >= $%d`, paramNum))
			*args = append(*args, f.UpdatedAt.Between.Start)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.updated_at <= $%d`, paramNum))
			*args = append(*args, f.UpdatedAt.Between.End)
		}
		if f.UpdatedAt.IsNull != nil {
			if *f.UpdatedAt.IsNull {
				conditions = append(conditions, `tenant_user.updated_at IS NULL`)
			} else {
				conditions = append(conditions, `tenant_user.updated_at IS NOT NULL`)
			}
		}
	}
	if f.Id != nil {
		if f.Id.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.id = $%d`, paramNum))
			*args = append(*args, f.Id.Eq.ToUUID())
		}
		if len(f.Id.In) > 0 {
			placeholders := make([]string, len(f.Id.In))
			for i, id := range f.Id.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, id.ToUUID())
			}
			conditions = append(conditions, fmt.Sprintf(`tenant_user.id IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.Id.IsNull != nil {
			if *f.Id.IsNull {
				conditions = append(conditions, `tenant_user.id IS NULL`)
			} else {
				conditions = append(conditions, `tenant_user.id IS NOT NULL`)
			}
		}
	}
	if f.DisplayName != nil {
		if f.DisplayName.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.display_name = $%d`, paramNum))
			*args = append(*args, *f.DisplayName.Eq)
		}
		if len(f.DisplayName.In) > 0 {
			placeholders := make([]string, len(f.DisplayName.In))
			for i, value := range f.DisplayName.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, value)
			}
			conditions = append(conditions, fmt.Sprintf(`tenant_user.display_name IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.DisplayName.ILike != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.display_name ILIKE $%d`, paramNum))
			*args = append(*args, "%"+*f.DisplayName.ILike+"%")
		}
	}
	if f.Version != nil {
		if f.Version.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version = $%d`, paramNum))
			*args = append(*args, *f.Version.Eq)
		}
		if f.Version.Gt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version > $%d`, paramNum))
			*args = append(*args, *f.Version.Gt)
		}
		if f.Version.Gte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version >= $%d`, paramNum))
			*args = append(*args, *f.Version.Gte)
		}
		if f.Version.Lt != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version < $%d`, paramNum))
			*args = append(*args, *f.Version.Lt)
		}
		if f.Version.Lte != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version <= $%d`, paramNum))
			*args = append(*args, *f.Version.Lte)
		}
		if f.Version.Between != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version >= $%d`, paramNum))
			*args = append(*args, f.Version.Between.Min)
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user._version <= $%d`, paramNum))
			*args = append(*args, f.Version.Between.Max)
		}
	}
	if f.TenantID != nil {
		if f.TenantID.Eq != nil {
			paramNum++
			conditions = append(conditions, fmt.Sprintf(`tenant_user.tenant_id = $%d`, paramNum))
			*args = append(*args, f.TenantID.Eq.ToUUID())
		}
		if len(f.TenantID.In) > 0 {
			placeholders := make([]string, len(f.TenantID.In))
			for i, id := range f.TenantID.In {
				paramNum++
				placeholders[i] = fmt.Sprintf("$%d", paramNum)
				*args = append(*args, id.ToUUID())
			}
			conditions = append(conditions, fmt.Sprintf(`tenant_user.tenant_id IN (%s)`, strings.Join(placeholders, ", ")))
		}
		if f.TenantID.IsNull != nil {
			if *f.TenantID.IsNull {
				conditions = append(conditions, `tenant_user.tenant_id IS NULL`)
			} else {
				conditions = append(conditions, `tenant_user.tenant_id IS NOT NULL`)
			}
		}
	}

	if len(conditions) == 0 {
		return ""
	}

	return " WHERE " + strings.Join(conditions, " AND ")
}
