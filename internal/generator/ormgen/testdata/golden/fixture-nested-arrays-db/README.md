# fixture-nested-arrays-db ORM

Generated Go ORM for the `fixture-nested-arrays-db` schema. **DO NOT EDIT** -- this
module is generated by `superschematic build`; change the schema source instead.

Generated at: 2026-01-02T03:04:05Z

## Contents

- `database.go` -- connection pool, transactions, user context
- `interfaces.go` -- repository/database/transaction interfaces + no-op implementations
- `query.go` -- typed field selectors, filters, update structs, snapshot builders, and `ApplyTo`
- `utils.go` -- shared query helpers
- `repository_board.go` -- Board repository

## Usage

```go
import (
	orm "example.com/schemas/orm/fixture-nested-arrays-db"
	types "example.com/schemas/types/go/fixture-nested-arrays-db"
)

db, err := orm.Connect(ctx, connString)
if err != nil {
	return err
}
defer db.Close()

record, err := db.Board.GetOne(ctx, id, nil)
```

Repositories expose `GetOne`, `GetManyByIDs`, `FindOne`, `FindMany`,
`CreateOne`, `CreateMany`, `UpdateOne`, `UpdateMany`, `DeleteOne`, and
`DeleteMany`. Tables with soft deletes additionally expose `HardDeleteOne`.

Each `*Update` type has `ApplyTo(*types.T)` to project set and `SetNull`
fields onto a stored row, and `New*SnapshotUpdate` to build a full
replacement from a complete value.
